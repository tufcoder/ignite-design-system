import { Meta } from '@storybook/blocks'
import { fontWeights } from '@tufcoder-ignite-ui/tokens'

import { TokensGrid } from '../../components/TokensGrid'

<Meta title="Ignite-UI/Tokens/Font Weights" />

# Font Weights

Essas são as cores utilizadas no Ignite UI.

<TokensGrid tokens={fontWeights} hasRemValue />
